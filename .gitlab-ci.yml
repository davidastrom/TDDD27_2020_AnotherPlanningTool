stages:
    - build
    - test
    - deploy

image: docker/compose:latest

services:
    - docker:dind

before_script:
    - docker login -u qnott -p $DOCKER_KEY
    - docker info

build:
    stage: build
    script:
        - docker-compose build
        - docker-compose up -d
